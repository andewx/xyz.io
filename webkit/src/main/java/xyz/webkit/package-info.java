/**
 * This module package provides utility functionality that should in general be
 * available to the xyz.controller backend logic. Here we provide the main implementation
 * of a SiteTemplating class which parses HTML files for @:SomeKey and provides the user
 * a mechanism to replace those keys allowing for controller HTML file construction.
 */
package xyz.webkit;